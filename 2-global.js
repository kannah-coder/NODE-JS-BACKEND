//GLobals -NO window

// _dirname-path to the current ddirectory
//_filename-file name
//require-function to use modules(commonJs)
//module-info about current module(file)
//process-info about env where the program is being excited
console.log(__dirname);

setInterval(()=>{
    console.log("hello world");
},1000);